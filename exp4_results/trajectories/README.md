# Experiment 4 trajectory arrays

The full Experiment 4 neural trajectory arrays are not included in this GitHub release. They total approximately 105 GB of generated binary data.

The final saved CSV outputs used in the manuscript are included under `exp4_results/`, together with the figures, scripts, and release manifests. The repository therefore supports audit of the reported results without placing the full trajectory archive in Git history.

The full trajectory archive is maintained separately. This repository documents the analysis pipeline and preserves the frozen result tables used in the manuscript.
