# Trajectories

Full neural trajectory arrays are not included in this GitHub release because they are large binary generated data.

The derived CSV outputs used in the manuscript are included in the release. Full trajectory arrays can be regenerated using the provided scripts or stored separately in an external data archive.
